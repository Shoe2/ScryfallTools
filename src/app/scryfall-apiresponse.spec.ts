import { ScryfallAPIResponse } from './scryfall-apiresponse';

describe('ScryfallAPIResponse', () => {
  it('should create an instance', () => {
    expect(new ScryfallAPIResponse()).toBeTruthy();
  });
});
